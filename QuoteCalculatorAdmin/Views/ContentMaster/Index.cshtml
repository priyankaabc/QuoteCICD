@using QuoteCalculatorAdmin.Models;
@*@using QuoteCalculatorAdmin.Data;*@

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">Heading Content</h5>
        <a href="#" onclick="openheadingContentModal(0)" data-backdrop="static" data-toggle="modal" data-target="#ChildEditDetailPopup" class="btn btn-warning"><i class="icon-plus2"></i> Add Heading Content</a>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-sm-12">
                <div class="table-responsive">
                    <table id="tblMastercontentList" class="table dataTable nowrap" width="100%">
                        <thead>
                            <tr>
                                <th scope="col">Heading</th>
                                <th scope="col">Heading Content</th>
                                <th scope="col">Country</th>
                                <th scope="col">Quote Type</th>
                                <th scope="col">Company</th>
                                <th scope="col">Display Order</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var item in Model)
                                {
                                    <tr>

                                        <td class="text-overflow">
                                            <p>@Html.DisplayFor(modelItem => item.Heading)</p>
                                        </td>
                                        <td>
                                            <p class="text-overflow">@Html.DisplayFor(modelItem => item.HeadingContent)</p>
                                        </td>
                                        <td>
                                            <p class="text-overflow">@Html.DisplayFor(modelItem => item.CountryCode).ToString().Replace(",", ", ")</p>
                                        </td>
                                        <td>
                                            <p>@Html.DisplayFor(modelItem => item.QuoteType)</p>
                                        </td>
                                        <td>
                                            <p>@Html.DisplayFor(modelItem => item.Company)</p>
                                        </td>
                                        <td>
                                            <p>@Html.DisplayFor(modelItem => item.DisplayOrder)</p>
                                        </td>
                                        <td>
                                            <a href="#" onclick="openheadingContentModal(@item.HeadingContentId)" data-toggle="modal" data-target="#contentmodalPopup" class="btn-edit"><i class="fa icon-pencil"></i></a>
                                            <a href="/ContentMaster/ManageContent?contentId=@item.HeadingContentId" class="btn-delete"><i class="fa icon-trash"></i></a>
                                        </td>
                                    </tr>
                                }*@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>

<div class="modal" id="contentmodalPopup" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style=" top: 5% !important;">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <span class="h4 mb-0" id="myModalLabel">Heading Content</span>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="divContentmodalPopup">
                </div>
            </div>

        </div>
    </div>
</div>
<script src="~/Scripts/Pages/HeadingContent.js"></script>


<script>

       @*function HeadingContentPopUp(HeadingContentId) {
             $.ajax({
                 url: '@Url.Action("HeadingContentOpen", "ContentMaster")?HeadingContentId=' + HeadingContentId,
                 async: false,
                 type: 'GET',
                 success: function (data) {
                     if (data) {
                         $('#contentmodalPopup').modal('show');
                         $('#divContentmodalPopup').html(data);
                     }
                 }
             });
        }*@


        //function getTheSubstring(value, length) {
        //    if (value != null && value.length > length)
        //        return kendo.toString(value.substring(0, length)) + "...";
        //    else return kendo.toString(value);
        //}
</script>